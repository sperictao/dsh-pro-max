# v0.1.1

应用图标更新：替换为全新图标，macOS 采用圆角矩形底座样式，全平台安装包与托盘/窗口图标统一。

## Changes

- **新应用图标**：全套尺寸重新生成（macOS `.icns`、Windows 多尺寸 `.ico`、Linux/Store PNG 全规格），小尺寸下线稿依然清晰可辨

## 验证

- `pnpm run check:release -- --tag v0.1.1` 通过
