# Product

## Register

product

## Users

使用 DeepSeek Harness（dsh）CLI 的开发者与自托管用户。他们在自己的 Mac（少量 Windows）上把这个 Tauri 桌面启动器当作 dsh web 服务的管理面板：一键安装/启动/停止/重启本地服务（127.0.0.1:3899），或在 Tailscale tailnet 内配置带身份授权的远程 HTTPS 访问。使用场景是「任务中」：打开应用 → 看一眼状态 → 偶尔切换访问模式或修复版本栈 → 回到 dsh 本身的工作流。多数窗口非最大化（960×680 默认），偶有宽屏/最大化。

## Product Purpose

dsh-pro-max 是 DeepSeek Harness 的桌面启动器（launcher）：把 dsh CLI + 授权插件 + Tailscale serve 的多步安装与启动链路收进一个可信赖的一键流程。成功的标准是：用户不看文档就能把服务跑起来；出任何故障时，页面上的步骤时间线能给出「问题 + 下一步」；状态一眼可读（本地/远程、运行/停止、哪个环节卡住）。

## Brand Personality

干净（Clean）、友好（Friendly）、自由（Freedom）——来自仓库 AGENTS.md 的三条设计理念，优先级即序：

- **Clean**：单一事实来源，无冗余分叉；界面不堆积装饰，信息密度克制。
- **Friendly**：失败路径有明确去向（problem/solution 成对出现），不弹窗打断，不裸抛异常；新用户无需逆向代码。
- **Freedom**：状态语义（绿=运行中、红=危险）跨 42 个主题族保持稳定，不为主题牺牲可读性。

三词人格：可靠、安静、直给。界面的最大字号永远留给「当前状态」，而不是品牌口号。

## Anti-references

- SaaS 营销页习气：渐变文字、玻璃拟态、每节一个全大写 eyebrow、装饰性动效。
- 工程调试面板习气：纯文本状态、按钮无主次、禁用态一坨半透明色块、中英文混排。
- 过度组件化：嵌套卡片、为单一用途发明的奇怪控件。标准场景用标准控件（系统 toggle、原生按钮语义）。

## Design Principles

1. **状态即界面**：首页最大的元素是服务当前状态；任何时刻用户三秒内能答出「跑没跑、本地还是远程、卡在哪一步」。
2. **失败可操作**：每个失败状态必须伴随可执行的下一步（按钮或指引文案），而不是只报错。
3. **跨族稳健**：一切颜色走 token（42 主题族 × 亮暗都要成立）；禁用态落 muted 灰，危险语义靠结构（实心/描边）而非色相深浅。
4. **词汇统一**：与设置/市场/模型页共享同一套按钮配方、卡片语言、内容区节奏（p-6、rounded-xl 区块卡、header-btn 导航）；首页不发明新控件。
5. **语言无泄漏**：zh 界面下不出现未翻译英文（含动态 key）；状态、时间轴、模式文案全部进词典。

## Accessibility & Inclusion

- 目标 WCAG AA：正文 ≥4.5:1，大字 ≥3:1；42 族 × 亮暗逐一验证高风险组合（禁用态、状态点、destructive）。
- 所有动画（脉冲、spinner）带 `prefers-reduced-motion` 降级。
- 交互控件保持原生语义（label 包裹 toggle、button role），键盘焦点环（focus-visible:ring）不丢。
