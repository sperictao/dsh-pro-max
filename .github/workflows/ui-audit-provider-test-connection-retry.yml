name: UI Audit Provider Test Connection Retry

on:
  pull_request:
    paths:
      - "src/features/models/**"
      - "src/shared/i18n/**"
      - "e2e/capture-models-test-connection-retry.mjs"
      - ".github/workflows/ui-audit-provider-test-connection-retry.yml"
  workflow_dispatch:

jobs:
  provider-test-connection-retry:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v4
        with:
          version: 10.15.1
      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: "pnpm"
      - run: pnpm install
      - name: Run provider test connection recovery tests
        run: pnpm vitest run src/features/models/ProviderDialog.test-connection-retry.test.tsx src/features/models/ProviderDialog.edit.test.tsx
      - name: Install Playwright video encoder
        run: pnpm dlx playwright@1.62.1 install ffmpeg
      - name: Capture provider test connection recovery flow
        run: node e2e/capture-models-test-connection-retry.mjs
      - name: Convert recording to GIF
        shell: bash
        run: |
          if ! command -v ffmpeg >/dev/null 2>&1; then
            sudo apt-get update
            sudo apt-get install -y ffmpeg
          fi
          ffmpeg -y \
            -i .artifacts/e2e/models-test-connection-retry/models-test-connection-retry.webm \
            -vf "fps=10,scale=960:-1:flags=lanczos,split[s0][s1];[s0]palettegen=max_colors=128[p];[s1][p]paletteuse=dither=bayer" \
            .artifacts/e2e/models-test-connection-retry/models-test-connection-retry.gif
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: ui-audit-models-test-connection-retry
          path: .artifacts/e2e/models-test-connection-retry/
          if-no-files-found: warn
          retention-days: 14
