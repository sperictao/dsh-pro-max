# 壳通知仅限进程事故

接入 notification 插件，但触发点只限受管子进程的生命周期事故（意外退出、启动失败）。硬需求来自自启动：静默启动到托盘后主窗口可能从不出现，进程挂了若无系统通知，用户无从得知。

**Considered Options**：

- 漂移恢复也弹通知 —— 拒绝。漂移在 60 秒轮询内被自动修复，弹通知纯打扰；且看守域已有「不弹通知」语义边界（CONTEXT.md），不为壳域开口子。
- updater 事件弹通知 —— 拒绝。更新检查由用户手动发起，UI 已覆盖进度与结果，无后台场景。
